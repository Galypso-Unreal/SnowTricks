<div class="trick-teaser col-md-2 col-lg-2">
    <a class="name h3_style" href="{{path('trick', {slug: trick.slug })}}">
        <h2>{{trick.name}}</h2>
    </a>
    <div>
        {{trick.description}}
    </div>
</div>